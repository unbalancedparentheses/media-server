# Runtime data (lives in ~/media/, not in this repo)
/media/
*.log

# Nix build artifacts
result
result-*
.direnv/

# macOS
.DS_Store

# Secrets — NEVER commit these
# If you need to share config.json, remove passwords first
# config.json is tracked but contains default passwords — change them!
